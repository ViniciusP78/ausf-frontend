import { makeStyles } from '@material-ui/core';

export default makeStyles(theme => ({
  menu: {
    boxShadow: '0px 2px 11px rgba(0, 0, 0, 0.09)',
    borderRadius: 5,
    padding: '12px 24px',
    marginRight: 12,
  }
}));